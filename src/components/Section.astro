---
import Button from "../components/Button.astro";
import CarruselCircularTailwind from "./CarruselCircularTailwind";
---

<section
  class="flex items-center min-h-[calc(100vh-1px)] px-6 bg-"
>
  <div class="container mx-auto text-center">
    <h1
      class="text-3xl tracking-tight font-light text-balance text-[#f8f8ff] sm:text-5xl"
    >
      Sé que necesitas vender con una <span class="text-[#f8f8ff]"
        >landing page</span></h1><br />
    <p class="text-neutral-500 sm:text-xl">
      Convierte clicks en clientes y visitas en ventas aumentando tráfico
      filtrado a tu chat de <span class="text-[#25d366]">WhatsApp</span>
    </p><br />
    <div class="">
      <Button texto="Comenzar ahora" />
    </div>
    <div>
    <CarruselCircularTailwind client:load/>
  </div>
  </div>
</section>

<style>
  /* Variables CSS para responsividad 3D */
  :root {
    --carrusel-radio-z: 150px; /* Móvil: Radio pequeño (solo se ve el frente) */
    --carrusel-perspective: 800px;
  }

  /* Aplica los estilos para Escritorio (sm: breakpoint) */
  @media (min-width: 640px) { 
    :root {
      --carrusel-radio-z: 250px; /* Escritorio: Radio grande (se ven los divs vecinos) */
      --carrusel-perspective: 1200px;
    }
  }
</style>
